openapi: 3.1.0
info:
  title: Ecommarce API
  version: 1.0.0
  description: Versioned REST API serving both storefront and admin. Skeleton at Step 1; filled per step.
servers:
  - url: /api/v1
tags:
  - name: health
  - name: auth
  - name: catalog
  - name: inventory
  - name: search
  - name: promotions
  - name: cms
  - name: crm
  - name: orders
  - name: payments
  - name: courier
  - name: rma
  - name: reviews
  - name: notifications
  - name: analytics
  - name: admin
paths:
  /health:
    get:
      tags: [health]
      summary: Liveness check
      responses:
        '200':
          description: OK
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT