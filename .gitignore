node_modules/
dist/
.next/
coverage/
.turbo/
*.tsbuildinfo
.env
.env.local
.env.*.local
.DS_Store
*.log

# Step 3: local media uploads (dev only, Step 12 swaps to S3)
uploads/


# Temp scratch (Step 9+)
apps/api/prisma/_step9_parts/


# ---------------------------------------------------------------------------
# Terraform — never commit state/providers/plans (contains secrets)
# ---------------------------------------------------------------------------
**/.terraform/
**/.terraform.lock.hcl
**/*.tfstate
**/*.tfstate.*
**/*.tfstate.backup
**/*.tfplan
**/tfplan
**/tfplan_*
**/.backup-*/
**/.backup-*
**/crash.log
**/crash.*.log

# Backup files created by fix scripts
*.bak-*
*.bak
*.backup
**/*.bak-*
